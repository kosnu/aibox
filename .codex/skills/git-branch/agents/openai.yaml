interface:
  display_name: "Create Branch"
  short_description: "Create feature branches with or without an issue"
  default_prompt: "Use $git-branch to create a feature branch from an issue number or a short branch topic."

policy:
  allow_implicit_invocation: true
