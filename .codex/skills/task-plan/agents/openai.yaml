interface:
  display_name: "Task Plan"
  short_description: "Investigate and design an approval-ready implementation plan"
  default_prompt: "Use $task-plan to inspect this work, parallelize investigation with subagents, model the changed behavior, and stop after an approval-ready plan."

policy:
  allow_implicit_invocation: true
