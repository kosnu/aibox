interface:
  display_name: "Task Plan"
  short_description: "Investigate, review, and design an approval-ready implementation plan"
  default_prompt: "Use $task-plan to inspect this work, parallelize investigation with subagents, review the plan with senior-role subagents, and stop after an approval-ready plan."

policy:
  allow_implicit_invocation: true
