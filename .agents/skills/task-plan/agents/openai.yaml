interface:
  display_name: "Task Plan"
  short_description: "Plan repository work with outcome, scope, verification, and budgeted review"
  default_prompt: "Use $task-plan to ground this task, define the outcome, scope, success criteria, verification, and budgeted review, then stop for approval before editing."

policy:
  allow_implicit_invocation: true
