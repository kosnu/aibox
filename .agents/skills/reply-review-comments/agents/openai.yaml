interface:
  display_name: "Reply Review Comments"
  short_description: "Reply to clearly handled PR review threads"
  default_prompt: "Use $reply-review-comments to inspect PR review threads, reply only where handling is clear, include attributable commit IDs when relevant, resolve only fully complete threads, and report skipped threads."

policy:
  allow_implicit_invocation: true
